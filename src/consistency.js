export const CONSISTENCY=Object.freeze({LOCAL:'local',EVENTUAL:'eventual',BOUNDED_STALENESS:'bounded_staleness',READ_YOUR_WRITES:'read_your_writes',CAUSAL:'causal',QUORUM:'quorum',SERIALIZABLE:'serializable'});
export function consistencyContract(profile,options={}){if(!Object.values(CONSISTENCY).includes(profile))throw new TypeError('unknown consistency profile');const base={profile,allowsStaleReads:false,requiresCoordination:false,sessionMonotonic:false};switch(profile){case CONSISTENCY.LOCAL:return{...base,scope:'process'};case CONSISTENCY.EVENTUAL:return{...base,allowsStaleReads:true,scope:'replica'};case CONSISTENCY.BOUNDED_STALENESS:return{...base,allowsStaleReads:true,maxStalenessMs:positive(options.maxStalenessMs,5000)};case CONSISTENCY.READ_YOUR_WRITES:return{...base,sessionMonotonic:true};case CONSISTENCY.CAUSAL:return{...base,sessionMonotonic:true,causal:true};case CONSISTENCY.QUORUM:return{...base,requiresCoordination:true,quorum:true};default:return{...base,requiresCoordination:true,serializable:true};}}
function positive(v,d){if(v==null)return d;if(!Number.isFinite(v)||v<0)throw new TypeError('invalid staleness bound');return v;}
